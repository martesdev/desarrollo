# .env.example - plantilla unificada para el proyecto
# No incluyas este archivo con credenciales reales en el repo. Copia a `.env` y rellena valores sensibles.

# --- Base de datos (prefijo DB_)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=nombre_base_datos
DB_USERNAME=usuario_bd
DB_PASSWORD=contraseña_bd

# Opciones adicionales (ejemplo)
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_unicode_ci

# --- Correo / SMTP (prefijo MAIL_)
MAIL_TEST_MODE=true
MAIL_TEST_ADDRESS=tu_correo_de_prueba@dominio.com
MAIL_DRIVER=smtp
MAIL_HOST=smtp.office365.com
MAIL_PORT=587
MAIL_ENCRYPTION=tls
MAIL_USERNAME=usuario_smtp@dominio.com
MAIL_PASSWORD=tu_contraseña_smtp
MAIL_FROM_ADDRESS=no-reply@tu-dominio.com
MAIL_FROM_NAME="Nombre del remitente"

# --- Opcionales para integraciones / tokens
# JWT (firebase/php-jwt)
JWT_SECRET=tu_clave_secreta_jwt
JWT_TTL=3600

# --- Config generales (ambiente)
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost

# --- Notas:
# - Copia este archivo a `.env` y rellena con valores reales. No comitees `.env`.
# - Si mantienes archivos `.env` por servicio, puedes usar este como referencia para variables necesarias.
